# Server Configuration
PORT=4000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/tiny-inventory

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# CORS (comma-separated). Set to your web URL in production so API accepts browser requests.
CORS_ORIGIN=http://localhost:3000

# Frontend API URL (build-time for web). Set to your API URL in production.
VITE_API_URL=http://localhost:4000
